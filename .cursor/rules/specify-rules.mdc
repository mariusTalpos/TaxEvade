---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---
# TaxEvade Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-04

## Active Technologies
- TypeScript 5.4+ + Angular 18+ (standalone components, signals), Angular Material or CDK for dialogs (if used) (001-csv-ledger-import)
- IndexedDB (browser); transaction store with indexes on date, account, and composite for deduplication (001-csv-ledger-import)
- TypeScript 5.4+ + Angular 18+ (standalone components, signals), Angular Material or CDK (tables, dialogs if needed) (002-guided-transaction-classification)
- IndexedDB (browser); extends existing transaction store with classification fields and suggestion metadata; no new stores for rules (built-in heuristics are code/config). (002-guided-transaction-classification)
- TypeScript 5.4+ + Angular 18+ (standalone components, signals), Angular Material or CDK (tables, pagination, sort, filter) (003-classified-transactions-view)
- IndexedDB (browser); reuses existing transaction store and classification fields from 002; no new stores. (003-classified-transactions-view)
- IndexedDB (browser); reuses existing transaction store and classification fields from 002/003; no new stores. (004-consolidate-classification-view)
- TypeScript 5.5+, Angular 18+ + Angular 18 (standalone components, signals, router, HttpClient), IndexedDB (browser), Ollama (local HTTP for AI) (004-consolidate-classification-view)
- IndexedDB (transaction store with classification/suggestion fields); no new stores for 004 (004-consolidate-classification-view)

- TypeScript 5.4+ (Angular 18+) + Angular 18+ (standalone components, signals, Angular CLI or Nx) (001-csv-ledger-import)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

npm test; npm run lint

## Code Style

TypeScript 5.4+ (Angular 18+): Follow standard conventions

## Recent Changes
- 004-consolidate-classification-view: Added TypeScript 5.5+, Angular 18+ + Angular 18 (standalone components, signals, router, HttpClient), IndexedDB (browser), Ollama (local HTTP for AI)
- 004-consolidate-classification-view: Added TypeScript 5.4+ + Angular 18+ (standalone components, signals), Angular Material or CDK (tables, pagination, sort, filter)
- 003-classified-transactions-view: Added TypeScript 5.4+ + Angular 18+ (standalone components, signals), Angular Material or CDK (tables, pagination, sort, filter)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
